# TODO Set up the turtles and start button
# TODO make the turtles interactable and when they are clicked the computer choses a random option
# TODO Code the key presses for the bird to flap
# TODO End screen with scores if you die
import turtle as trtl
wn = trtl.Screen()
import random

trtl.addshape("P", ((0,3), (.5,2), (1,3), (2,3), (2,-3), (-2,-3), (-2,3)))
trtl.addshape("R", ((0,-2), (-3,-2), (-4,0), (-2,1), (-1,1), (1,2), (2,1), (3,1), (4,0), (3.5,-2)))

options = ("rock", "paper", "scissors")
player = None
comp = random.choice(options)

rock1 = trtl.Turtle(shape="R")
paper1 = trtl.Turtle(shape="P")

start_button = trtl.Turtle()
start_button.shape("square")
start_button.write("Click to Start", align="center", font=("Arial", 16, "bold"))
start_button.color("Black")
start_button.shapesize(2)
start_button.penup()
start_button.goto(0,-35)

rock1.hideturtle()
paper1.hideturtle()

#game functions
def game_start(x,y):
    start_button.hideturtle()
    start_button.clear()
    rock1.penup()
    rock1.showturtle()
    rock1.shapesize(10)
    rock1.goto(-100,0)
    rock1.left(90)
    paper1.penup()
    paper1.showturtle()
    paper1.shapesize(10)
    paper1.goto(0,0)
    paper1.left(90)
    
def turtle_clicked(x,y):
    print(f"Player: {player}")
    print(f"Computer: {comp}")
    trtl.Turtle(color="red")

rock1.onclick(turtle_clicked)(color="red")
paper1.onclick(turtle_clicked)




start_button.onclick(game_start)

wn.mainloop()